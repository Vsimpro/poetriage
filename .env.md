# API
HOST=0.0.0.0
HOST_PORT=5000
PORT=5000
MAX_UPLOAD_MB=200

# Flask/session
SECRET_KEY=

# Initial admin files are stored under ./data.
SECRET_KEY_FILE=./data/.secret_key
ADMIN_CREDS_FILE=./data/admin_credentials.txt

# Shared runtime storage
DATABASE_PATH=./data/database.db
UPLOAD_DIR=./data/uploads

# Pi runtime
PI_CODING_AGENT_DIR=./data/pi-agent
PI_CODING_AGENT_SESSION_DIR=./data/pi-sessions
PI_REPORT_TOOL=/app/pi-report-tool/index.ts

# Pi inference settings
# These seed the database on first run. After that, AppSettings in SQLite is the source of truth.
PI_PROVIDER=openrouter
PI_MODEL=deepseek/deepseek-v4-flash
PI_OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
PI_OPENROUTER_API_KEY=

# REMnux MCP
PI_REMNUX_CONTAINER=remnux-pi
PI_REMNUX_TIMEOUT=600
